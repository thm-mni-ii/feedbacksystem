import { NgModule } from "@angular/core";
import { RouterModule, Routes } from "@angular/router";

/**
 * SQL Playground Routing Module
 * 
 * This will be populated in Step 6 when components are moved.
 * For now, it's an empty shell to establish the module structure.
 */

const routes: Routes = [
  // Routes will be added in Step 6
];

@NgModule({
  imports: [RouterModule.forChild(routes)],
  exports: [RouterModule],
})
export class SqlPlaygroundRoutingModule {}
